[project]
name = "receipt-constrained-test"
version = "0.1.0"
description = "Receipt formatter with constrained approval tests"
requires-python = ">=3.12"
dependencies = [
    "approvaltests>=18.0",
    "pytest>=9.1.0",
]
